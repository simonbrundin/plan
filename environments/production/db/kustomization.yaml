apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../common/db

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: plan-db-schema
    files:
      - schema.sql=schema.sql

patches:
  - path: instances-patch.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: plan-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: AUTH_DISABLED
              value: "false"

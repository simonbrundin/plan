apiVersion: v1
kind: Service
metadata:
  name: plan-api-service
  labels:
    app: plan-api
spec:
  selector:
    app: plan-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../common/db

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: plan-db-seed
    files:
      - seed-data.sql=seed-data.sql

patches:
  - path: patch.yaml

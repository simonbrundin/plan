apiVersion: db.atlasgo.io/v1alpha1
kind: AtlasSchema
metadata:
  name: plan-db-schema
  namespace: plan
spec:
  urlFrom:
    secretKeyRef:
      key: uri
      name: plan-db-admin
  schema:
    configMapKeyRef:
      key: schema.sql
      name: plan-db-schema
  schemas:
    - "public"

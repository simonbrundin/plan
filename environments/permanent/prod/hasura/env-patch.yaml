apiVersion: apps/v1
kind: Deployment
metadata:
  name: hasura
  namespace: plan
spec:
  template:
    spec:
      containers:
        - name: hasura
          envFrom:
            - secretRef:
                name: hasura-env

name: CI
on: [push]

permissions:
  contents: read
  packages: write

jobs:
  commit:
    uses: simonbrundin/deployment-pipeline/.github/workflows/build.yaml@main
    with:
      dockerfile-path: frontend
